stereo:
  min_disparity: 20
  num_disparities: 96  # Multiplo de 16
  block_size: 7         # 3-7 suele ir bien 5?
  p1_factor: 8
  p2_factor: 24
  disp12_max_diff: 1 #PROBAR CON 2
  uniqueness_ratio: 25
  speckle_window_size: 100
  speckle_range: 2 #  Si usas SGBM, a veces hay que multiplicarlo por 16.
  mode: "HH"     #    mode_map = {"SGBM": 0, "HH": 1, "SGBM_3WAY": 2}
  max_depth_meters: 3.2

wls:
  lambda: 2000.0   #  (probar 5000)
  sigma: 0.8       # Sensibilidad al color (1.0 - 2.0)