stereo:
  min_disparity: 0
  num_disparities: 128  # Multiplo de 16
  block_size: 7         # 3-7 suele ir bien 5?
  p1_factor: 8
  p2_factor: 32
  disp12_max_diff: 3 #PROBAR CON 2
  uniqueness_ratio: 10
  speckle_window_size: 100
  speckle_range: 2 #  Si usas SGBM, a veces hay que multiplicarlo por 16.
  mode: "MODE_HH"
  max_depth_meters: 4

wls:
  lambda: 8000.0   #  (probar 5000)
  sigma: 0.8       # Sensibilidad al color (1.0 - 2.0)